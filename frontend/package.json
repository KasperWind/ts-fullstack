{
  "scripts" : {
    "build" : "npm run clean && tsc && npm run copy-files",
    "clean" : "rm -rf ./dist",
    "copy-files" : "cp ./src/public/* ./dist/ -r && mkdir ../backend/dist/public && cp ./dist/* ../backend/dist/public/ -r"
  }
}
